package com.projectmanager.mysql;

//import java.util.Optional;


//import static org.junit.jupiter.api.Assertions.assertEquals;
       
//import static org.mockito.Mockito.*;
import static org.junit.jupiter.*;

import org.junit-jupter.*;

//import org.junit.jupiter.api.BeforeEach;
//import org.junit.jupiter.api.Test;
//import org.mockito.*;
  

 

public class UsuarioRepositoryTest {


/*
    @Mock
    private UsuarioRepository usuarioRepository;

    private Usuario usuario;

    @BeforeEach
    public void setup() {
        MockitoAnnotations.openMocks(this);
        usuario = new Usuario(1L, "Fabiano");

        // Mockando o comportamento do repositório
        when(usuarioRepository.findById(1L)).thenReturn(Optional.of(usuario));
        when(usuarioRepository.save(usuario)).thenReturn(usuario);
        when(usuarioRepository.update(1L, "Novo Nome")).thenReturn(new Usuario(1L, "Novo Nome"));
    }

    @Test
    public void testCreateUsuario() {
        Usuario usuarioCriado = usuarioRepository.save(usuario);
        assertNotNull(usuarioCriado);
        assertEquals("Fabiano", usuarioCriado.getNome());
    }

    @Test
    public void testReadUsuario() {
        Optional<Usuario> usuarioEncontrado = usuarioRepository.findById(1L);
        assertTrue(usuarioEncontrado.isPresent());
        assertEquals("Fabiano", usuarioEncontrado.get().getNome());
    }

    @Test
    public void testUpdateUsuario() {
        Usuario usuarioAtualizado = usuarioRepository.update(1L, "Novo Nome");
        assertNotNull(usuarioAtualizado);
        assertEquals("Novo Nome", usuarioAtualizado.getNome());
    }

    @Test
    public void testDeleteUsuario() {
        // Não há nada para verificar aqui, mas podemos simular a ação
        doNothing().when(usuarioRepository).delete(1L);
        usuarioRepository.delete(1L);
        verify(usuarioRepository, times(1)).delete(1L); // Verifica se o delete foi chamado
    }
}

 */   
    
    
    
}
